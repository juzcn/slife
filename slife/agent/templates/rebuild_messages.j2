▸ Context recall

The turn's context is rebuilt before it runs. Choose which history the turn
needs.

Current user input:
{{ user_input }}

Reply with ONE JSON object holding the recall parameters below, and nothing
else. An empty object means "no search" — take the most recent turns instead.

{{ recall_params }}
