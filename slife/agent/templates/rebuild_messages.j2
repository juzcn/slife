▸ Context recall

This call chooses the context the turn runs on: the turns you name become the
context, in place of the ones in hand.

Current user input:
{{ user_input }}

The query is matched against stored turns — their user messages, the tools
they called, their answers. Name what the turn needs, in the words a stored
turn would contain:

  input 那人工智能学院呢？ — the subject is in the conversation above
    → {"query": "首经贸 人工智能学院 成立"}

  input 刚才问首经贸管工学院院长是谁？ — that turn has left the context
    → {"query": "首经贸 管理工程学院 院长"}

  input 这两天都做了些什么？ — a period, and no topic
    → {"since": "yesterday"}

  input 上周关于校庆都说了什么？ — a topic within a period
    → {"query": "首经贸 校庆", "since": "last week"}

  input 把这段话翻成英文 — the turn reads on its own
    → {}

Reply with ONE JSON object holding the parameters of the tool below, and
nothing else. An empty object means no recall is needed — the context
already in hand is enough for this turn.

{{ tool_schema }}
