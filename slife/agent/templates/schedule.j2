You are executing the scheduled task "{{ name }}".

Task description:
{{ description }}

Carry out the task fully, using whatever tools you need. When done, you MUST finish up as follows:

1. Write the report: draft a one-line title that summarizes the result (e.g. "{{ name }}: {{ month_day }} summary") and a complete, well-structured Markdown body (sections/lists/tables).
2. Complete the run: call report_save(name="{{ name }}"{% if due_at %}, due_at="{{ due_at }}"{% endif %}, title=<your one-line title>, content=<your Markdown body>).
3. Notify the user that the task is done, then close with one or two sentences on what you did and the report title.

All three steps are part of the task — do not skip them. On partial failure, report it honestly and save the report anyway.