▸ 当前状态
   - 当前时间: {{ current_datetime }} (UTC{{ utc_offset }})
{%- if context_time_start %}
   - 上下文覆盖: {{ context_time_start }} ~ 现在
{%- endif %}
{%- if model_name %}
   - 已切换模型: {{ model_name }}（上限: {{ context_window }} tokens，输入: {{ input_modalities }}）
{%- endif %}
{%- if cwd %}
   - 已在新的工作目录: {{ cwd }}
{%- endif %}
{%- if shell %}
   - 已切换到 Shell: {{ shell }}
{%- endif %}
   - 上轮 Token: {{ last_total_tokens }} ({{ last_usage_pct }}%)
{%- if presence_events %}
▸ 最近 peer 上线/下线
{%- for ev in presence_events %}
   - {{ ev.time }} {{ ev.text }}
{%- endfor %}
{%- endif %}
